# Dependencies
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output
.next
out

# Environment files — secrets must be injected at runtime via Docker env vars
.env
.env.*
!.env.example

# Git
.git
.gitignore

# Dev / local tooling
.vscode
.idea
*.log

# OS
.DS_Store
Thumbs.db

# Test / CI artifacts
coverage
.nyc_output

# Large dev-only files (Excel source files for build:census — not needed at runtime)
public/data/publications/*.xlsx
scripts/

# Docker / server-side infrastructure (not part of the app image)
Dockerfile
docker-compose*.yml
.dockerignore
